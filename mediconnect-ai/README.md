# MediConnect AI (Frontend)

React + Vite frontend for the MediConnect AI dashboard.

## Getting started
- cd mediconnect-ai
- npm install
- npm run dev

## Configuration
Create a `.env.local` (not committed) with:
- VITE_API_BASE_URL=http://localhost:8000/api
